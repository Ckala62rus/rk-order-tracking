#### mirror repository

- git clone --mirror https://ckala62rus@bitbucket.org/ckala62rus/rk.git
- git remote set-url --push origin https://github.com/Ckala62rus/rk-order-tracking.git
- git fetch -p origin
- git push --mirror

#### update mirror repository

- git fetch -p origin
- git push --mirror
